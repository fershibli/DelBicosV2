import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  webviewContainer: {
    position: 'absolute',
    bottom: 20,
    right: 20,
    width: 120,
    height: 120,
    borderRadius: 10,
    overflow: 'hidden',
  },
});
